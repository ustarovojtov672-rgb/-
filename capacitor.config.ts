import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.prilozyxa.calories",
  appName: "Prilozyxa Calories",
  webDir: "out",
};

export default config;
